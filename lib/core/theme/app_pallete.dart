import 'dart:ui';
import 'package:flutter/material.dart';

class AppPallete{
  // static const Color backgroundColor = Color.fromRGBO(24, 24, 32, 1);
  // static const Color navigationbackgroundColor = Color.fromRGBO(27, 18, 18, 1);
  //
  // // Gradients (you can keep these)
  // static const Color gradient1 = Color.fromRGBO(187, 63, 221, 1);
  // static const Color gradient2 = Color.fromRGBO(251, 109, 169, 1);
  // static const Color gradient3 = Color.fromRGBO(255, 159, 124, 1);
  //
  // static const Color borderColor = Color.fromRGBO(52, 51, 67, 1);
  //
  // // Core Colors
  // static const Color whiteColor = Colors.white;
  // static const Color greyColor = Colors.grey;
  // static const Color errorColor = Colors.redAccent;
  // static const Color transparentColor = Colors.transparent;
  //
  // // *** Add these! ***
  // static const Color accentColor = Color(0xFF00E676); // Example: A vibrant green (customize this!)
  // static const Color textColor = Color(0xFFEEEEEE); // Example: Light text for dark background (customize!)
  // static const Color secondaryTextColor = Color(0xFF9E9E9E); // Example: Slightly darker text for less emphasis
  //
  // // Example: More specific color roles (optional but recommended)
  // static const Color primaryButtonColor = accentColor;
  // static const Color secondaryButtonColor = greyColor;
  // // static const Color backgroundColor = Color.fromRGBO(24, 24, 32, 1);
  // // static const Color navigationbackgroundColor = Color.fromRGBO(27, 18, 18, 1);
  // // static const Color gradient1 = Color.fromRGBO(187, 63, 221, 1);
  // // static const Color gradient2 = Color.fromRGBO(251, 109, 169, 1);
  // // static const Color gradient3 = Color.fromRGBO(255, 159, 124, 1);
  // // static const Color borderColor = Color.fromRGBO(52, 51, 67, 1);
  // // static const Color whiteColor = Colors.white;
  // // static const Color greyColor = Colors.grey;
  // // static const Color errorColor = Colors.redAccent;
  // // static const Color transparentColor = Colors.transparent;
  // Base colors
  static const Color backgroundColor = Color.fromRGBO(24, 24, 32, 1);
  static const Color navigationbackgroundColor = Color.fromRGBO(27, 18, 18, 1);

  // Gradients
  static const Color gradient1 = Color.fromRGBO(187, 63, 221, 1);
  static const Color gradient2 = Color.fromRGBO(251, 109, 169, 1);
  static const Color gradient3 = Color.fromRGBO(255, 159, 124, 1);

  // Structure colors
  static const Color borderColor = Color.fromRGBO(52, 51, 67, 1);
  static const Color dividerColor = Color.fromRGBO(52, 51, 67, 1);
  static const Color cardColor = Color.fromRGBO(30, 30, 40, 1);
  static const Color shadowColor = Color.fromRGBO(0, 0, 0, 0.2);

  // Core Colors
  static const Color whiteColor = Colors.white;
  static const Color greyColor = Colors.grey;
  static const Color errorColor = Colors.redAccent;
  static const Color transparentColor = Colors.transparent;

  // Brand colors
  static const Color accentColor = Color(0xFF00E676); // Primary green accent
  static const Color secondaryAccentColor = Color(0xFF1DE9B6); // Secondary green accent

  // Text colors
  static const Color textColor = Color(0xFFEEEEEE); // Primary text - light
  static const Color secondaryTextColor = Color(0xFF9E9E9E); // Secondary text - medium grey
  static const Color tertiaryTextColor = Color(0xFF616161); // Tertiary text - darker grey

  // Functional colors
  static const Color iconColor = Color(0xFFBDBDBD); // Default icon color
  static const Color activeColor = accentColor; // Active state
  static const Color inactiveColor = Color(0xFF757575); // Inactive state
  static const Color successColor = Color(0xFF00C853); // Success messages/indicators
  static const Color warningColor = Color(0xFFFFD600); // Warning messages/indicators
  static const Color dangerColor = Color(0xFFFF5252); // Danger/error messages
  static const Color infoColor = Color(0xFF40C4FF); // Information messages

  // Button colors
  static const Color primaryButtonColor = accentColor;
  static const Color secondaryButtonColor = greyColor;
  static const Color disabledButtonColor = Color(0xFF424242);

  // Form colors
  static const Color inputBackgroundColor = Color(0xFF2A2A38);
  static const Color inputBorderColor = Color(0xFF3D3D4E);
  static const Color focusedInputBorderColor = accentColor;

  // Chart and data visualization colors
  static const Color positiveChangeColor = Color(0xFF00E676); // Green for positive values
  static const Color negativeChangeColor = Color(0xFFFF5252); // Red for negative values
  static const Color neutralChangeColor = Color(0xFFBDBDBD); // Grey for neutral values

  // Notification colors
  static const Color notificationBadgeColor = Color(0xFFFF5252);
}